export type UserType = {
  id: string;
  firstName: string;
  lastName: string;
  email: string;
  documentNumber: string;
}

export type UserInputDTO = {
  firstName: string;
  lastName: string;
  email: string;
  documentNumber: string;
}